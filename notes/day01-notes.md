# Day1 学习笔记

## 知识点
1. JDK / IDEA 安装
2. HelloWorld 程序
3. 变量 / 数据类型
4. 单行注释：
    
       //   
5. 多行注释：

       /* */
6. 文档注释：
 
       /**

       *这是一个xxx 

       */
7. 字面量：
    1. 整数字面量 123 直接写
    2. 浮点数字面量 123.45 直接写
    3. 字符字面量 'a'
    4. 字符串字面量 "hello"
    5. 布尔字面量 true false
    6. 空字面量 null
    7. 特殊字面量 \t Tab缩进 \n 换行 
8. 变量：是用来装数据的容器，变量要有名称，要有数据类型，要有值  
   格式：**数据类型 变量名 = 值**  
   变量可以改变值，但类型不能改变。  
  
9. 数据类型   

| 数据类型    | 数据范围byte                           |
|---------| -------------------------------------- |
| byte    | -126~127                               |
| short   | -32768~32767                           |
| int（默认） | -2147483648~2147483647（10位数，21亿） |
| long    | -2^63~ 2^63-1                          |
| float   | -3.4E+38 ~ 3.4E+38（4字节）            |
| double（默认） | -1.7E-308 ~ 1.7E+308                   |
| char    | 0 - 65535                              |
| boolean | true\false                             |

10. 关键词 具有意义的单词 不能来作为标识符来定义变量 例如：class、public、static、void  
标识符：不能以数字开头，不能包含空格，不能包含特殊字符，不能包含中文

# 小题
## Java是值传递还是引用传递
只有值传递，没有引用传递  
基本类型传递的是“值的拷贝”（改了不影响原变量）

引用类型传递的是“地址值的拷贝”（改对象内容会影响，但改引用本身不会影响）  

## Java 支持哪些基本数据类型？各占多少字节？
Java 中有 8 种基本数据类型，分别为：
- 6 种数字类型：
- 4 种整数型：byte(1字节)、short(2字节)、int(4字节)、long(8字节)
- 2 种浮点型：float(4字节)、double(8字节)
- 1 种字符类型：char(2字节)
- 1 种布尔型：boolean(1字节)  本质其实是用的int（4字节）




